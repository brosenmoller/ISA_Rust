use bevy::prelude::*;

mod game_over_menu;
mod pause_menu;
mod hud;

pub struct GameUIPlugin;

impl Plugin for GameUIPlugin {
    fn build(&self, app: &mut App) {
        // app
        //     .add_plugins()
        // ;
    }
}